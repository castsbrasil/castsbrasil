<div class="col-sm-6 col-md-offset-3">
  <div class="page-header">
    <h2><%= t('.resend_unlock_instructions', :default => "Resend unlock instructions") %></h2>
  </div>

  <%= simple_form_for(resource, as: resource_name, url: unlock_path(resource_name), method: :post) do |f| %>
      <%= f.input :email %>

      <%= f.button :submit, t('.resend_unlock_instructions', :default => "Resend unlock instructions"), class: 'btn-primary pull-right' %>
  <% end %>
  <br class="clearfix" />

  <%= render "devise/shared/links" %>
</div>