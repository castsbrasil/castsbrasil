<!-- Section Register -->
<section class="board-home">
  <div class="container">
    <% if user_signed_in? %>
      <%= render 'signed_in' %>
    <% else %>
      <%= render 'not_signed_in' %>
    <% end %>
  </div>
</section>
<!-- /Section Register -->

<!-- Section Latest -->
<section class="featured latest-home">
  <div class="container">
    <%= render 'shared/cast_list', casts: @casts.most_recent, title: 'Últimos screencasts publicados' %>
  </div>
</section>
<!-- /Section Latest -->

<!-- Section Latest -->
<section class="featured most-watched-home">
  <div class="container">
    <%= render 'shared/cast_list', casts: @casts.most_viewed, title: 'Screencasts mais visualizados' %>
  </div>
</section>
<!-- /Section Latest -->
